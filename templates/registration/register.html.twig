{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<style>
/* Ensure the body and container have proper margin and padding */
body {
    margin: 0;
    padding: 0;
    display: flex; /* Use flexbox to center content */
    justify-content: center; /* Center horizontally */
    align-items: center; /* Center vertically */
    height: 100vh; /* Full viewport height */
    
}

.flexbox {
    display: flex; /* Use flexbox to align items side by side */
    justify-content: space-around; /* Add space around items */
    align-items: flex-start; /* Align items to the top */
    padding: 20px; /* Add padding for overall spacing */
    max-width: 80%; /* Limit width of the container */
    background-image: url("/images/back.png");/* Background color for contrast */
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
    border-radius: 10px; /* Add rounded corners */
}

.text-wrapper {
    max-width: 35%; /* Increase width of the text section */
    padding-right: 20px; /* Add padding to the right for space */
}

.form-wrapper {
    max-width: 100%; /* Adjust width to align properly with text section */
    padding-right: 100px;
     padding-left: 100px;
     padding: 70px;
      /* Add padding for space */
      /* Add padding for space */
    background: linear-gradient(135deg, #3498db, #8e44ad);
    border-radius: 10px; /* Add rounded corners to match container */
}

/* Style for the h1 and h3 elements */
h1, h3 {
    padding: 2vh;
    font-size: 1em; /* Font size for h1 */
    font-weight: bold;
    color: #2C3E50; 
    margin-bottom: 20px;
    background: linear-gradient(135deg, #3498db, #8e44ad); 
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

/* Different sizes for h1 and h3 */
h1 {
    font-size: 2em;
}

/* Adjust the form input elements and button */
.form-wrapper .form-group {
    margin-bottom: 15px; /* Add margin to space out form fields */
}

.form-wrapper input[type="text"],
.form-wrapper input[type="email"],
.form-wrapper input[type="password"] {
    width: 100%; /* Make input fields take full width of the parent */
    padding: 10px; /* Add padding to make the input fields larger */
    font-size: 1.2em; /* Increase font size */
    border: 1px solid #ccc; /* Add border */
    border-radius: 5px; /* Add rounded corners */
    box-sizing: border-box; /* Ensure padding and border are included in the element's total width and height */
}

#button {
    /* Button background color */
    background-color: #1abc9c; /* Match the extra color */
    color: white; /* Button text color */
    padding: 10px 20px; /* Button padding */
    border: none; /* Remove border */
    border-radius: 5px; /* Rounded corners */
    cursor: pointer; /* Pointer cursor on hover */
    width: 100%; /* Full width button */
    margin-top: 10px;
}

.form-wrapper input[type="file"] {
    display: none; /* Hide the original file input */
}

.form-wrapper label[for="registration_form_image"] {
    display: inline-block;
    padding: 10px 20px;
    margin: 10px;
    background-color: #1abc9c; /* Match the extra color */
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    width: 100%;
    text-align: center;
}

.form-wrapper label[for="registration_form_image"]:hover {
    background-color: #18927a; /* Darker background on hover */
}

.form-group {
    margin-top: 10px;
}

#container{
    display:block;
    width:100%;
    margin-bottom: 30px;
}

</style>

<div class="flexbox">
    <div class="text-wrapper">
        <h1>Register</h1>
        <h3>To get started and enjoy all the features we offer, please create an account by filling out the registration form below. 
        Just provide your email address, choose a strong password, and complete the required fields to set up your profile. Once registered, 
        you’ll gain access to exclusive content and personalized experiences. We value your privacy and security, so rest assured that
        your information is protected with us. If you have any questions or need assistance, feel free to contact our support team. 
        Join us today and be part of our community!</h3>
    </div>
    <div class="form-wrapper">
        {{ form_errors(registrationForm) }}

        {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.email) }} 
            {{ form_row(registrationForm.username) }}
            {{ form_row(registrationForm.plainPassword, { label: 'Password' }) }}

            <div class="form-group">
                <label for="registration_form_image">Choose a profile picture</label>
                {{ form_widget(registrationForm.image, { 'attr': {'style': 'display:none;'} }) }}
            </div>
            {{ form_row(registrationForm.agreeTerms) }}
            <button type="submit" class="btn" id="button">Register</button>
        {{ form_end(registrationForm) }}
    </div>
</div>
{% endblock %}
